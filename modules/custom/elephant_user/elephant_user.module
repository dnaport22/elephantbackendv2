<?php

use Drupal\elephant_user\Service\ActivateAccount;


/**
 * Implement hook_tokens_values.
 */
function elephant_user_tokens_values($type, $object = NULL, $options = array()) {
  if ($type == 'user') {
    $user = $object;
    $tokens['foo'] = $user->foo;
    return $tokens;
  }
}